version: '2.1'

services:
  facade:
    build: ../composer-facade
    mem_limit: 512m
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker

  composition:
    build: ../composition-service
    mem_limit: 512m
    environment:
      - SPRING_PROFILES_ACTIVE=docker
